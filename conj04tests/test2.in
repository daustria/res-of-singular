LIB "resolve.lib";

int t = timer;

ring A = (0,complex),(x(0..1),y(0..1)),lp;

ideal J = x(0)^5 + x(1)*y(1);

printf("Polynomial:%s", J[1]);
printf("First center of blowup:%s", Center(J));

list l = resolve(J);

presentTree(l);

printf("Time taken:%s", timer - t);

quit;
