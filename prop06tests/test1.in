LIB "resolve.lib";

ring A = (0, complex),(x(0..5), w), lp;

int t = timer;
poly f = x(1)^12+x(2)^12+x(3)^12+x(4)^12+x(5)^12+x(0)^4; //max length 3

ideal J = f*w;

printf("polynomial: %s", J[1]);
printf("first center of blowup: %s", Center(J));

list l = resolve(J);

printf("time taken: %s", timer - t);

presentTree(l);
quit;
