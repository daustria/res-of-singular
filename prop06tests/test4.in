LIB "resolve.lib";

ring A = (0, complex),(x(0..2), w), lp;

poly f = x(0)^2 + x(1)^2 + x(2)^2; //max length 1 
ideal J = f*w;

printf("polynomial: %s", J[1]);
printf("first center of blowup: %s", Center(J));

list l = resolve(J);

presentTree(l);
quit;
